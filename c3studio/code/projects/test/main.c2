import core.crt;
import geo.rect;
import geo.point;

// Common messages generated by type
enum messages {
	Errors, Warnings, States
};

// Test comment -
// Ability used this type
enum abilities {
	Strenght, Dexterity, Constitution, Intellegence, Wisdow, Charisma,
};

int main() {
	// Новый комментарий
	rect rc;
	rc.x1 = 0;
	rc.x2 = rc.x1;
	char* test = "Здесь русская строка текста";
	int t1 = rc.x1 + 10;
	int t2 = 10 + rc.x1; // Same ast test
	int t3 = 2*(5 + rc.x1/2);
	crt.exit(0);
}

// Print function ++
void print(char* text, int v) {
}