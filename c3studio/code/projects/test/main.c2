import core.crt;
import geo.rect;

// Common messages generated by type
enum messages {
	Errors, Warnings, States
};

// Ability used this type
enum abilities {
	Strenght, Dexterity, Constitution, Intellegence, Wisdow, Charisma,
};

void print(char* text, int v) {
}

int main() {
	rect rc;
	rc.x1 = 0;
	rc.x2 = rc.x1;
	char* test = "Test another";
	int t1 = rc.x1 + 10;
	int t2 = 10 + rc.x1; // Same ast test
	int t3 = 2*(5 + rc.x1/2);
	crt.exit(0);
}